# Data

This directory is reserved for local prompt corpora, generated paraphrases, and benchmark
fixtures that are too large or too environment-specific to commit.

The first implementation ships a small customer-support seed fixture for classifier
pipeline smoke checks. It is not large enough for quality claims. It also ships a cosine
baseline fixture so future classifier work can measure improvement against a known failure
mode.
